server:
  port: 8080
spring:
  config:
    activate:
      on-profile: dev
openapi:
  service:
    api-docs: api-service
    title: API document
    version: v1.0.0
    server: http://localhost:${server.port}

